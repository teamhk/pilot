<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hk.pilot.mapper.AuthMapper">

<insert id="addMember" >
	INSERT All 
	INTO MEMBERS (ID, PWD, NAME, EMAIL, PNUM, USERZIPCODE, USERFIRSTADDR, USERSECONDAADDR, USEREXTRAADDR, GRADE) VALUES (#{id}, #{pwd}, #{name}, #{email}, #{pnum}, #{userZipCode}, #{userFirstAddr}, #{userSecondAddr}, #{userExtraAddr}, #{grade})
	INTO personalpay (id, cardcom, cardnum)	VALUES (#{id}, #{cardCom}, #{cardNum})
	SELECT * FROM DUAL
</insert>

<!-- id중복체크 sql문 -->
<select id="checkId" parameterType="String" resultType="com.hk.pilot.dto.Members">
	select * from members where id=#{id}
</select>

<select id="addOwner">
	INSERT ALL
	INTO MEMBERS (ID, PWD, NAME, EMAIL, PNUM, USERZIPCODE, USERFIRSTADDR, USERSECONDAADDR, USEREXTRAADDR, GRADE) VALUES (#{id}, #{pwd}, #{name}, #{email}, #{pnum}, #{userZipCode}, #{userFirstAddr}, #{userSecondAddr}, #{userExtraAddr}, #{grade})
	INTO STORES (ID, SNAME, STOREZIPCODE, STOREFIRSTADDR, STORESECONDAADDR, STOREEXTRAADDR, SCONTACT, SNUM, BANK, BNUM)
	INTO ITEMLIST (SNUM, SHIRT, BLOUSE, T_SHIRT)
	SELECT * FROM DUAL
</select>

</mapper>
	